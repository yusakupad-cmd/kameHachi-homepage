# HANDOFF — kameHachi-homepage

更新: 2026-08-18 / 更新者: Codex

> **使い方**: Claude は「調べて決めて書く」、Codex は「作る」。両者はこのファイルで現在地を引き継ぐ。
> 作業を終えたら、AI・人間どちらであっても最後にこのファイルを上書き更新する。履歴は Git に任せ、ここには**今の状態だけ**を書く。

## 現在地

`ogp.jpg`（1200×630）と `functional-training-generated.png`（1448×1086）を旧作業コピーから救出し、SHA-256 一致を確認してコミット済み（`37c37d0`）。`functional-training-generated.png` はページから参照していない。

`index.html` は決定済みの3点だけ反映済み。

- 住所表示に `〒421-1114` を追加
- 指定住所のGoogleマップ直リンクを追加（`target="_blank" rel="noopener"`）
- 電話リンク2箇所に `aria-label="054-648-3030へ電話する"` を追加
- 電話番号は構造化データ・リンクとも `054-648-3030` を維持。旧コピーの050番号は含まれない
- `python3 -m http.server 8877` で実表示確認済み
- 1440×900と390×844で横はみ出しなし、ローカル画像の読込失敗なし
- Googleマップリンクから指定住所のGoogleマップが新規タブで開くことを確認済み

旧作業コピー（7/21）と正本（7/26）の差分358行について、**外部の一次情報で裏取りを完了した。**

### 電話番号 — 決着済み。正本が正しい

| 出典 | 電話番号 |
|---|---|
| 厚労省「介護サービス情報公表システム」（事業所番号 2275301691-00） | **054-648-3030** |
| 亀八茶屋 公式サイト www.8kame.com | **054-648-3030** |
| 正本 `index.html` | **054-648-3030** ✅ |
| 旧コピー `index.html` | 050-648-3030 ❌ |

補足として、`050-648-3030` は形式そのものが不正。日本の IP 電話 050 番号は `050-XXXX-XXXX`（4桁-4桁）であり、3桁-4桁の組み合わせは存在しない。一方 `054` は静岡MAの市外局番で、藤枝市岡部町はこれに該当する。**旧コピー側の 050 は採用しない。**

### 旧コピーから復活させるべきもの（裏取り済み）

| 項目 | 判断 | 根拠 |
|---|---|---|
| `〒421-1114` | **復活すべき** | 介護サービス情報公表システムの登録値と一致。正本では欠落している |
| Googleマップへの直リンク | **復活すべき** | 正本には iframe 埋め込みしかない。スマホで地図アプリを起動して経路案内を出す導線が失われている。来訪者は高齢者のご家族が中心 |
| 電話リンクの `aria-label` 2箇所 | **復活すべき（ただし番号は 054 に直す）** | 正本は `aria-label` が1件のみ（メニュー開閉）。旧は3件。介護事業所のサイトであり、スクリーンリーダー利用者への配慮を落とすべきではない |

### 旧コピーから復活させなくてよいもの（意図的な変更と確認済み）

| 項目 | 判断 | 根拠 |
|---|---|---|
| 独自 Instagram フィード（`about-instagram-grid`） | **復活不要** | 正本は Behold ウィジェット（`<behold-widget feed-id="VrYAtRNLeLZYaly5nOuG">`）へ移行済み。7/26 のコミット「Instagram全幅→本文の縦積みレイアウトに変更」がこれ |
| `id="news"` セクション | **復活不要** | 正本に `href="#news"` の残存リンクなし。削除は整合が取れている |
| `id="youtube"` セクション | **復活不要** | 同上 |
| `about-meta` の住所表記 | **正本を維持** | 正本は「岡部町羽佐間641」まで記載。旧は「岡部町」止まり。正本の方が正確 |
| マップ埋め込みのクエリ | **正本を維持** | 正本は住所クエリ、旧は緯度経度。住所クエリの方が保守しやすい |

## 次の1手

mainへのpush後、公開サイトでOGP画像、住所、電話リンク、Googleマップ導線を確認する。

## 完了条件

- [x] 上記3点が反映され、電話番号は全箇所 `054-648-3030` のまま
- [x] スマートフォン幅で横はみ出しがない
- [x] Googleマップリンクが指定住所のGoogleマップを開く
- [x] `ogp.jpg` が `og:image` / `twitter:image` / 構造化データから参照されている
- [ ] main push後の公開サイト確認

## 触らないこと

- **電話番号**（外部一次情報で確定済み。旧コピーの値に戻さない）
- `hero-*.jpg` / `activity-*.jpg` の既存画像（再取得困難）
- Behold ウィジェットの `feed-id`
- `worker/wrangler.toml`

## 未解決の判断（人間が決める）

- ローカル表示では既存の `favicon.ico` が404。faviconを新規作成するかは別タスク

## 決定事項（2026-08-18・小川さん判断）

- **`functional-training-generated.png` は参照せずリポジトリに残す。** ページには出さない。
  - 理由: クライアントに見せる内容の変更は AI の判断で行わない。ファイルは保全されるので、掲載したくなった時点で参照を追加すれば足りる
  - **AI はこの画像を `index.html` に勝手に追加しないこと**
- **push 方針。** 挙動を変えない変更（docs・設定・アセット）は `main` 直 push 可。機能変更は PR
- **実表示確認の手段。** Codex のアプリ内ブラウザが `file://` を拒否したため未完了。`python3 -m http.server` でローカルサーバーを立てて確認するのが確実
