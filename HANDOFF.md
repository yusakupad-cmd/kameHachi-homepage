# HANDOFF — kameHachi-homepage

更新: 2026-08-18 / 更新者: Codex

> **使い方**: Claude は「調べて決めて書く」、Codex は「作る」。両者はこのファイルで現在地を引き継ぐ。
> 作業を終えたら、AI・人間どちらであっても最後にこのファイルを上書き更新する。履歴は Git に任せ、ここには**今の状態だけ**を書く。

## 現在地
- 旧コピーから `ogp.jpg` と `functional-training-generated.png` を救出済み。コピー前後のSHA-256一致を確認した
- `ogp.jpg` は1200×630 JPEG、`functional-training-generated.png` は1448×1086 PNG
- `index.html` の `og:image` / `twitter:image` / 構造化データは、公開URL配下の `ogp.jpg` を参照している
- `index.html` 自体は変更していない
- 旧コピーとの比較は正本側基準で76行追加・282行削除。旧コピー側だけの実装が複数あるため、採否は人間判断待ち
- アプリ内ブラウザはローカル `file://` URLをセキュリティ制約で開けず、デスクトップ幅・スマートフォン幅の実表示確認は未完了

## 次の1手
人間が旧コピー側だけの `index.html` 差分を採用するか判断する。公開前にデスクトップ幅とスマートフォン幅で実表示を確認し、ローカルコミットの push は明示許可を得てから行う。

## 完了条件
- [x] `ogp.jpg` が本リポのルートに存在する
- [x] `index.html` から `ogp.jpg` が meta og:image として参照されている
- [ ] スマートフォン幅で表示崩れがない
- [x] 旧作業コピー側にしかない変更を抽出済み
- [ ] 旧作業コピー側だけの変更の採否を決定

## 触らないこと
- `hero-*.jpg` / `activity-*.jpg` の既存画像（再取得困難）
- `worker/wrangler.toml`（デプロイ設定。触る場合は必ず人間に確認）
- 人間判断前の `index.html` 差分取り込み
- 明示許可のない `main` への push

## 未解決の判断（人間が決める）
- 旧コピー側の電話番号 `050-648-3030` と正本側 `054-648-3030` のどちらを正とするか
- 旧コピー側の独自Instagramフィード、機能訓練画像参照、ページ内アンカー、郵便番号・Googleマップリンク、アクセシビリティ属性を採用するか
- ローカルファイルを開けるブラウザでのデスクトップ幅・スマートフォン幅の実表示確認
